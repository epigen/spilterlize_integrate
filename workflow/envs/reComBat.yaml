name: reComBat
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.10
  - scikit-learn=1.3.0
  - pandas=1.5.3
  - numpy=1.25
  - fire=0.6.0
  - tqdm=4.66.4
  - pip=24.0
#   - pip:
#     - recombat==0.1.4
#   - pip:
#     - --no-deps
#     - git+https://github.com/BorgwardtLab/reComBat --install-option="--no-deps" doesn't work
#     - recombat==0.1.4